export interface Imenu {
  id: number;
  name: string;
  description: string;
  photo: string;
  price: number; //from string to number error
  available: boolean;
  quantity: number; 
  subtotal: number; 
}
